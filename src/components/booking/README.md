# Booking components

Domain-specific building blocks for the booking flow (calendar cells,
slot pickers, booking cards, etc.). Add new components here as the
booking UI is broken down further. Keep them presentational — data
fetching stays in the route files or hooks under `src/hooks/`.